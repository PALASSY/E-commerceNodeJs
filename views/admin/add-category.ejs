<% layout('layout') -%>

<h1 class="text-center">Ajouter une categorie</h1>
<main>

  <!--Affichage du message flash de (user.js) si il y a erreur-->
    <% if(message.length > 0){ %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        <%= message%>
      </div>
    <% } %>
  <form method="post" class="mt-3">
      <div class="mb-3 row">
        <label for="name" class="col-sm-2 col-form-label text-end">Categorie:</label>
        <div class="col-sm-10">
          <input type="text"  class="form-control" id="name" name="name">
        </div>
      </div>
      <input type="submit" class="btn btn-info text-end btn-lg" value="Créer">
  </form>
</main>
